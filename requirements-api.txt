# Dependances minimales pour faire tourner l'API FastAPI en production.
# Exclut volontairement les libs de notebook (jupyter, matplotlib, seaborn),
# de tracking (mlflow, dvc), de tests (pytest, httpx) et les modeles non
# utilises par le final_model.pkl (xgboost, lightgbm).

# API REST
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# Manipulation de donnees
pandas==2.2.3
numpy==1.26.4

# ML : preprocessing et modele final (CatBoost)
scikit-learn==1.5.2
catboost==1.2.7
joblib==1.4.2
